import React, { Component } from 'react'
import PropTypes from 'prop-types'

class Tile extends Component {
    static propTypes = {
        prop: PropTypes
    }

    state = {

    }

    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default Tile;
